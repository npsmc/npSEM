language: python

before_install:
  - sudo apt-get install -y gfortran

addons:
  apt:
    packages:
    - gcc
  homebrew:
    packages:
    - gcc

os:
  - linux
  - osx

python:
  - "3.6"

install:
  - pip install .

script: pytest

